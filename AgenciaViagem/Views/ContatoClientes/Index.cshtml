@model IEnumerable<AgenciaViagem.Models.ContatoCliente>

@{
    ViewData["Title"] = "Index";
}

<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <style>
        body {
            background-color: rgb(238, 186, 42);
        }

        a {
            text-decoration: none;
            color: black;
        }

            a:hover {
                color: black;
            }
    </style>
</head>

<h1>Contatos dos clientes</h1>

<p>
    <a asp-action="Create">Cadastrar novo contato</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome_contato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.sobrenome_contato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.telefone_contato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cidade_contato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.regiao_contato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.email_contato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.motivo_contato)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome_contato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sobrenome_contato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.telefone_contato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cidade_contato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.regiao_contato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.email_contato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.motivo_contato)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_contato">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_contato">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_contato">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
